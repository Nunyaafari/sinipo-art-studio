scripts:
  buildCommand: npm run build
  runCommand: npm start

outputFiles:
  serverApp:
    include:
      - .

runConfig:
  minInstances: 1
  maxInstances: 1

env:
  - variable: JWT_SECRET
    secret: JWT_SECRET
    availability:
      - RUNTIME
