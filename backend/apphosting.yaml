scripts:
  buildCommand: npm run build
  runCommand: npm start

outputFiles:
  serverApp:
    include:
      - .

env:
  - variable: JWT_SECRET
    secret: JWT_SECRET
    availability:
      - RUNTIME
